---
import StoryBadge from './StoryBadge.astro';

interface Props {
  title: string;
  slug: string;
  date: Date;
  description: string;
  story: number;
}

const { title, slug, date, description, story } = Astro.props;

const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<article class="article-card">
  <h3><a href={`/articles/${slug}/`}>{title}</a></h3>
  <div class="meta">
    <time datetime={date.toISOString()}>{formattedDate}</time>
    &middot;
    <StoryBadge story={story} />
  </div>
  <p class="description">{description}</p>
</article>
